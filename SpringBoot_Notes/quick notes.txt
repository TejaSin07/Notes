quick notes

1) custome repo ->In a custom repository, we create a @Repository class, inject EntityManager provided by Spring, and use it to manually perform CRUD operations and queries.

2) Most built-in CRUD methods in CrudRepository operate using the primary key, and for non-ID fields, we define custom methods using naming conventions or @Query.

3) @Query("from Worker w where w.ename = :name and w.esalary = :salary")
List<Worker> getWorkersHQL(
    @Param("name") String name,
    @Param("salary") Double salary
);

4) @Query(value = "SELECT * FROM worker WHERE ename = :name AND esalary = :salary", nativeQuery = true)
List<Worker> getWorkersSQL( @Param("name") String name, @Param("salary") Double salary);

5) Sort sort = Sort.by("salary").descending();  //and sort by accept arguments in varArgs format 
List<Worker> list = repo.findAll(sort);

6) Pageable pageable = PageRequest.of(0{pageNumber}, 5{pagesize}, Sort.by("salary").descending()); 
Page<Worker> page = repo.findAll(pageable);

7) If no @Table or @Column is specified, JPA uses the class name as table name and field names as column names (Spring Boot usually converts them to lowercase/snake_case).

8) @GeneratedValue is designed for primary keys only, so for additional UUID fields you must generate them manually or use Hibernate-specific generators.

@Id
@GeneratedValue(strategy = GenerationType.IDENTITY)
private Integer id;

@Column(unique = true, nullable = false, updatable = false)
private UUID uuid;

@PrePersist
public void generateUUID() {
    if (uuid == null) {
        uuid = UUID.randomUUID();
    }
}


9) Validation should be applied on DTO  -> DTO ensures the client remains stable and independent from backend and database changes.

@PostMapping("/users")
public ResponseEntity<?> createUser(@Valid @RequestBody UserRequestDTO dto) {
    return ResponseEntity.ok("User created");
}
DTO Validation → @NotBlank, @NotNull, @Size, @Email, @Pattern, @Min/@Max → applied on fields → triggered using @Valid in controller → handled by Hibernate Validator

  
10) JSON → snake_case :: Java → camelCase

11) @JsonProperty is used to explicitly map JSON fields to Java fields, especially when names differ or when we want control over serialization/deserialization.

12) Java 8 + javax → legacy systems  :: Java 17 + jakarta → modern systems

13) circuit breaker -> spring-cloud-starter-circuitbreaker-resilience4j
spring-boot-starter-aop -> Recommended (practical use) -> spring-boot-starter-actuator
-> (if retry needed)resilience4j-retry

14) “How do you ensure your code works locally?”

I run the application locally and verify that it starts without errors.
I test APIs using tools like Postman or Swagger to validate end-to-end functionality.
I check database interactions and logs for correctness.
I also run unit tests to ensure business logic and edge cases are covered.
For critical flows, I rely on integration testing to validate the complete flow.

15) in git merge request even if we are merging our branches but only commits are update in main branch.

16) You get conflict only when your commit areas are updated by someone after your last pull;

17) 
Encryption: Converting data into a secure form using a key so only authorized parties can decrypt it.
Encoding: Transforming data into a different format for compatibility or transmission, which can be easily reversed without a key.
Hashing: Converting data into a fixed-length value using a one-way function that cannot be reversed to the original data.

18) Functional Testing	New feature correctness     {qa}
    Regression Testing	Existing feature stability   {qa}
    uat and performance  -> {staging}

19) feature → dev → staging → main → prod

20) API created -> PUT /api/roles/{roleId}/permissions => Role validation (isEditable{if system role}) → fetch existing permissions → compute add/remove (delta) →  update/insert (transactional) → cache invalidation (Redis) → audit logging

To delete key-value pair -> redisTemplate.delete("ROLE_PERMISSIONS:" + roleId);

21) @Async on method → return void / Future / CompletableFuture → enable using @EnableAsync → configure custom ThreadPoolTaskExecutor → use it via @Async("executorNameThreadPool") → ensures controlled thread usage instead of unbounded thread creation -> @retryable -> to retry 

22)  API → @Async → save history -> If app crashes → history lost

API → publish event → Kafka → consumer saves -> If app crashes -> event save on disk -> consumer process later

23) @ResponseStatus -> to send particular response status from method -> ignored when method not successful

24) @RequirePermission("WORKPAPER_UPLOAD")
@PostMapping("/assessments/{assessmentId}/workpapers")
public ResponseEntity uploadWorkpaper() {
    ...
}

25) Your Code → SLF4J (API) → Log4j2 (Implementation) → Output


26) optimistic -> just use @version in entity -> while inserting if version not matched  -> ObjectOptimisticLockingFailureException

27) pessimistic lock -> @lock(read or write lock) -> applied on repo method

28) “We introduce a new API version only when there are breaking changes such as field removal, renaming, structural changes, or behavioral changes. For backward-compatible changes like adding fields or new endpoints, we continue using the same version to avoid unnecessary version proliferation.”

29) 
Singleton Pattern
Proxy Pattern
Template Pattern
Builder Pattern
Observer Pattern
Decorator Pattern
Repository Pattern
DTO Pattern
circuit breaker

30) Shallow copy copies reference, deep copy copies object,

31) Business Use Case Identification -> Resource-Based URL Design (REST) ->Proper HTTP Methods
Request & Response Design -> API Versioning
Validation->Error Handling -> Security->Pagination & Filtering -> Idempotency -> API Documentation-> Performance Considerations -> Inter-service Communication

32) .ps1 file -> enter


33) path variable - > @pathvariable   :: query parameter - > @RequestParam

34) Why? Static methods are resolved using reference type, not object type 
Static methods are resolved at compile time (early binding)  -> Instance methods are resolved at runtime (dynamic binding)

35) super() present implicitly -> this() not present implicitly 
-> both should be first statement -> only one can be available at once
-> If parent has no default constructor, implicit super() fails → you must explicitly call super(params) or compilation breaks.

36) super -> require when current class and parent class variable has same name 

37) static block does not use this and super

38) method call statement bind at runtime dynamic method dispatch{Runtime polymorphism} :: A a = new B(); even if type is parent still use method body/method from child

39) while overriding return type should be same or covariant allowed only

40) access modifier should be wider in child not narrower

class Parent {
    Animal getAnimal() { return new Animal(); }
}

class Child extends Parent {
    @Override
    Dog getAnimal() { return new Dog(); } // allowed
}

41) exception should can be narrower not wider 

42) upcast :: parent ref = new child()
    downcast :: child ref2 = (child) ref;  Note :: only possible when upcasting already done

43) concrete method -> having body or having braces { } , abstract body -> no body 

44) submit method always return future object -> we can use both runnable and callable with executor service 

->ExecutorService executor = Executors.newFixedThreadPool(2);

Runnable task = () -> {
    System.out.println(Thread.currentThread().getName());
};

executor.submit(task);   // pass Runnable/callable object

or 

        Callable<String> task = () -> {
            String threadName = Thread.currentThread().getName();
            return "Executed by: " + threadName;
        };

        Future<String> future = executor.submit(task); // submit Callable
executor.shutdown();

45) to use completable future 

@Async
public CompletableFuture<String> getData() {
    return CompletableFuture.completedFuture("Hello");
}

46) ReentrantLock creation
ReentrantLock lock = new ReentrantLock(); // this is how we create ReentrantLock


lock.lock() -> to lock 
lock.unlock() -> used in finally to unlock 

47) Synchronization types

static synchronized void m1() {}  :: static method -> lock on ClassName.class

synchronized (ClassName.class) {// critical section} ::static block -> lock on ClassName.class

synchronized void m1() {}  ::// instance method -> lock on this (current object)

Object objectInstance = new Object();   ::// instance block -> lock on shared object

synchronized (objectInstance) {}  :: // you can pass any shared object (same reference across threads)


48) join() -> t1.start(); -> t1.join(); -> t2.start();

49) wait(),notify(),notify all() should be called inside synchronized "block or method " on the same object.

50) // Producer (read)
List<? extends Number> list;

// Consumer (write)   also read is possible with Object as type 
List<? super Integer> list;

51) volatile -> “This variable is shared across threads, and its value must always be read from main memory, not from thread-local cache.”

52) atomic integer -> AtomicInteger ensures thread-safe, lock-free updates (like increment) using CAS, preventing race conditions without synchronized.

53) Varargs :: void printNumbers(int... nums) => internally int... nums  ≈  int[] nums

54) Fail Fast behavior: => System throws ConcurrentModificationException exception immediately when modification or error occurs.   => ArrayList,HashMap
    Fail Safe behavior: => System avoids failure by operating on a copy or safe structure and continues execution. => ConcurrentHashMap

55) Producer and Consumer are created as separate threads using lambda expressions, both sharing a synchronized buffer where Producer adds data and Consumer removes it using wait() and notify() for coordination.  {we are using queue here to save data-> consumer : add :: producer : pop  }

56) HashMap ->  key → hashCode() →  hashing -> hash value(565136 ~ 14) → bit manipulation → bucket index

57) concurrent hashmap -> bucket level locking and used by spring internally to store singleton bean and bean defination catche

58) memory leaks  -> objects staying for longer than needed, so GC cannot reclain them -> static feild 
-> unclosed resources leak -> not doing catche eviction 

:: heap : object,instance variable :: stack : method calls(recursion),local variable:: method : class level data ,static variable  :: pc register : thread related ::  native stack area : 3rd party library/language data
 
=> gc works in heap area only  -> Unreachable objects (no references)

59) process :: ex program under main method 
    thread  :: multiple thread can create under main method/process

60) Integer is immutable so every operation create new variable

61) we not include static field in dto as it will break object design and not a good idea

62) serialization : FileOutputStream , ObjectOutputStream 
    de-serialization : FilInputStream , ObjectInputStream 
    transient  : wont take part in serilization
    final : will participate even if we use transient over that variable
    static : wont take part in serialization even if we use transient
    the order we maintain for serilization should maintain in deserilization

63) diamond problem only occure when we are implementing two interface having same default method 
same default method → override compulsory
specific parent call → InterfaceName.super.m1()
interface name in method call → resolve ambiguity
no InterfaceName.m1() → not static
super → call parent implementation

64) hashcode and equals :: to insert object of employee which has unique name only we overidde hashcode and equals method in employee class :: HashSet uses hashCode to find bucket and equals to detect duplicates — so fields used must be immutable to avoid data corruption.
->If you override equals() → you MUST override hashCode()

65) t1.join() pauses the calling thread (here, the main thread) at that line until t1 finishes.it will not pasuse other thread execution which already called previously.

66) set.add()  -> return boolean on insertion

67) java.lang imported automatically ex ,Object class,comparable
    java.util we need to import mannually ex, Collections,comparator

68) Your Code → JPA (EntityManager / Repository) → Hibernate → JDBC → DB

69) @Component → Redis helpers, JWT utils, Kafka producers ,AuditLogger,classes related to external client

70) Constructor called -> Dependency Injection ->  Aware Interfaces -> BeanPostProcessor (before init) -> @PostConstruct / init methods -> BeanPostProcessor (after init) -> Bean ready to use ->@PreDestroy / destroy methods

71)  If Y requires X to be injected, then X is the dependency and Y is the dependent (target) bean.

72) IOC -> principle to take care DI :: Spring bean -> classes managed by IOC

73) group Id -> company name in form of package :: artifactId : project name

74) bean lifecyle not managed by IOC for prototype beyond creation,when application context is closed then it will not call destroy automatically.

75)  Pom startes -> Simplied Maven dependencies -> group of relatable dependencies -> starter ,starter-web/data-japa/mail/security

76) spring boot(spring + auto config) adv -> starters , dependency version conflict solution -> autoconfig -> embedded servers -> actuators

77)  we added spring-boot-starter-web it uses below class to start IOC
Class Name : AnnotationConfigServletWebServerApplicationContext

78) where we customise bean creation -> security/kafka/redis/swagger customization

79) Starters and dependencies
---------------------------------------
spring-boot-starter -> spring-boot, spring-context, spring-aop, logging (logback, slf4j), snakeyaml

spring-boot-starter-web -> spring-web, spring-webmvc, jackson (JSON), validation, embedded tomcat

spring-boot-starter-data-jpa  -> spring-data-jpa, hibernate, jakarta persistence, jdbc

usually we add starter but we can seperate dependencies also like jakson

80) Auto Configuration  -> Boot will identify configurations based on pom starters

81) Actuators -> Starter actuator dependency ->  /health : Used by load balancers, Kubernetes  -> /metrics : Used for performance monitoring -> info : Custom app info -> beans :Shows all Spring beans -> mappings :all api endpoints
::management.endpoints.web.exposure.include=health,info

82) Run method -> bootstrap context -> start listeners -> prepare env -> create Ioc -> call runners -> start embded server->return Ioc

83) ApplicationRunner and CommandLineRunner : Both are Spring Boot interfaces used to execute code after the application starts and the ApplicationContext is fully initialized.
You implement their run() method to add startup logic.

84) If you don’t specify a name in @Component, Spring generates a default (lowercase)

85) @Qualifier : on injection point where @Autowired
    @Primary : on bean defination @Component ,@Repository

86) @Query ->  when method names cannot express complex queries

87) JpaRepo = CrudRepo + Pagination + Sorting + QBE
Specification = Separate interface (must be added explicitly)

88) QBE ignore null feilds only care about which feild are not null : we use Example.of(entity) 

89) JPA requires equals and hashCode for composite primary keys because it uses them to uniquely identify entities in the persistence context, cache, and collections.

90) hibernate need session to execute lazy loading hickariConnection only provide connection ,hickari : car ,hibernate : driver  QUE : why hibernate closes its session in lazy loading?

91) emails  -> mail starter -> configure application properties -> add our outlook mail -> create utils class -> use JavaMailSender 

92) utility -> helper : config -> configuration 

93) @ExceptionHandler(value = Exception.class) ::  @RestControllerAdvice

94) write query and path multiple time on page

95) why server side validation is reuire if we can apply validation on client side :: suppose makemytrip miss to apply validation ,whcih using irctc backend then issue is going to created thats why we apply validation on server side

96) http -> request : a) Request Line (Http Method Type + URL) -> Request Headers (Metadata) (Key-Value) -> c) Request Body (Payload - text/xml/json)

response :  Response Line (Status Code + Status MSG) ->  Response headers (Metadata - K & V) ->  Response body (Payload - text/xml/json)

content type- format of  request body : Accept - format you expect in the response

99)  consumes :: in which format reponse going to produces     
produces :: response 
ex : application/json , text/plain , 

100) xml :  before  java 9 automatically handle by jax b 
	    after java 9 we need jakson dependency to add in pom

101) @JsonProperty is used to map JSON fields to DTO fields during request handling and also to control how DTO fields are sent in the response JSON.

102) kubernetes acts as service registry ,no need to register services -> with dns it will work automatically

103) Admin Server is used to manage all backend apis actuator endpoints at one place ,we use admin starter server dependency in pom file -> and use @EnableAdminServer at start class
for clien we add admin-starter-client dependeny and add url config in prop file 

104) zipkin -> distributed tracing -> which api is taking more time -> micrometer  to generate span and trace id -> reporter brave -> for client just add url in prop file

105) @ExtendWith(MockitoExtension.class) -> unit test for service
@WebMvcTest(EmployeeController.class) -> for controller

106) during pr cicd apply test run on merged branch which will be the updated version of running branch 

107) 3 points -> 2 points dev + 1 points qa -> assigned to dev -> subtask like tesing qa

108)product owner tell ->  if requirenment change midway first i will do estimate and check if it is possible to complete in this period  -> if there is blocking story or if it require more time then i raised a risk in standup that it might take more time 

109) i  manage junior members and wsr and msr this how I take initiative apart from my role. and I dont help him directly while solving bugs I help him debuging without telling direct he used to connect with me 

110) one sprint -> two weeks -> 1 dev -> 8 to 10 story points

111)  looking for role with ownership

112) service boundry -> scop of responsibility and business capability -> own its data -> high cohesive (inside fuctionnality should be same)-> avoid exccesive syncronous calls -> independant deployment 

113) sonarqube -> bugs -> null pointer risk ,optional missuse
code smells ->  duplicate code,long method
vulnerabily -> security issue ,hardcoded values, loging sensetive data,missing authorization and authentication 


114) inside sorted(Comparator.comparing(Employee::getSalary).reversed() .thenComparing(Employee::getName)) this how we compare multiple feilds and get output

115) breakpoints work only when the application is running in debug mode.

116) npn run scriptname/dev

117) kub config maps :: service urls ,kafaka topicd ,timeout/retry config
kub secret :  jwt secret ,db user/password,api keys
aws secret manager :: highly sensetive ,prod db credentials ,third party api  credential


118) ingress  -> kubernetes eks -> nodes (ec2) -> pods -> containers -> image 

119) prod issue -> 5000 entries taking time to load ->  query written -> create connection -> preapareStatement(ps) -> resultset{rs} == ps.executeQuery() - > we run for loop on rs and map all 5000 one by one to dto -> but same in jpa -> issue is not that jpa or jdbc -> issue is, it is taking time as there is 5000 entries -> so we apply pagination by getting offset and  limit as input in method and update query -> and fetching 50 record per request .previously it was taking around 10-15 sec to load all now 100 to 400 ms.


120) Survey -> Questions -> Survey Assignment -> Survey Response :: Each (user_id, survey_id, question_id) combination must be unique -> APPLICATION-LEVEL SOLUTIONS WERE NOT USED -> as synchronization will reduce performance  ->Frontend Debouncing: -> Not reliable ->Cannot control retries or race conditions -> Added composite UNIQUE constraint:
ALTER TABLE survey_response -> ADD CONSTRAINT uk_user_survey_question -> UNIQUE (user_id, survey_id, question_id);

DBA team executed a controlled cleanup script to remove duplicates
After data was clean, you added a composite UNIQUE constraint via Flyway

121) topics divided into partition -> ordering guranded only inside one partitions -> Offset: Sequential number inside partition  
=> One consumer can read multiple partitions  :: One partition is consumed by ONLY ONE consumer (per group)


122) 1. Producer → audit-event-topic -> Consumer fails → retries → goes to audit-event-topic.DLT ->  DLQ Listener stores message (DB/map)->  Dev/Ops hits: POST /dlq/retry/{id} ->  Retry API fixes + republishes to audit-event-topic ->  Consumer processes successfully ->Reply event sent to audit-response-topic
{Message fails → goes to DLQ (topic.name.DLT) → stored → retry API (via UI/Postman) republishes → processed → optional reply event sent.}
@RetryableTopic(attempts = "3", dltTopicSuffix = "-dlt")

123) a) scenario where we are using kafka :In our audit system, we use Kafka to publish audit events like finding creation. Multiple services like notification, dashboard, and rule engine consume these events asynchronously. We implemented retry with backoff and DLQ handling to ensure fault tolerance and allow manual replay of failed events.


     b) when event or msg got failed :Email failure → SMTP/server issue
DB failure → Connection/timeout problem
Invalid data → Null/missing fields
Code error → Bug in consumer logic
API failure → Downstream service not available
Schema mismatch → Event structure changed
Timeout → Long processing / rebalance 

124) where we write retries :: on consumer service :: use fixedbackoff obj we pass waiting time and retries count

125) reply : “Reply in Kafka is implemented by consuming a message and producing a response to another topic, often using @SendTo in Spring Kafka, enabling asynchronous request-response communication between services.”
D
126) DLQ is a design pattern -> Originated from queue-based systems -> thats why we use queue to refer it 

127) retry  {}
 ->  @Retry(name = "xyService", fallbackMethod = "FallbackService")

->    // Fallback method (must have same params + Exception) IN BOTH CB and Retry
        public String sendFallback(String auditId, Exception ex) {

-> We configure retry in YAML by defining max attempts, wait duration between retries, and specifying which exceptions should trigger retries and which should be ignored.

128) CB AND RETRY 
  @Retry(name = "notificationService", fallbackMethod = "fallback")
    @CircuitBreaker(name = "notificationService", fallbackMethod = "fallback")

129)  Request comes → retry executes → if all attempts fail, circuit breaker records failure → once threshold is reached, circuit breaker opens → in OPEN state, the main method is not invoked and retry does not execute → retry works only in CLOSED and HALF-OPEN states.

130) retry -> audit service  using save call to save audit object at that time we may receive java.sql.SQLTransientConnectionException due to HikariCP  pool gets exhausted,so  we can use retry ove that save
a)
@Retry(name = "dbService", fallbackMethod = "saveAuditFallback") //here fallback woll throw excdption
    public Audit saveAudit(Audit audit) {

        System.out.println("Trying to save audit...");

        return auditRepository.save(audit); // DB call
    }

b) Audit Service → External Compliance API  if that service is down then we try first then we will throw error

131) where we apply both circuit breaker and retry

132) previously we are using memcatch

133) “In ConcurrentHashMap, writes are synchronized at bucket level, so only one thread can modify a bucket at a time. However, reads are mostly non-blocking and can happen concurrently even during writes.”

134) “Here, the pointcut is defined using an annotation-based expression @annotation(requirePermission). It captures all join points where methods are annotated with @RequirePermission. The @Before advice executes authorization logic before method execution.”

135)redisTemplate.opsForValue().get(key); // Fetch user role from Redis (O(1) lookup, avoids DB call)

redisTemplate.opsForValue().set(key, value, TTL); // Store user role with TTL to prevent stale cache (String key-value)

redisTemplate.opsForSet().members(key); // Get all permissions for a role (used for authorization check)

redisTemplate.opsForSet().add(key, values); // Store role permissions in Redis Set (ensures uniqueness + fast lookup)

136) Deadlock is a circular wait between concurrent transactions competing for locks, not just multiple transactions on the same database.If two methods having transaction annotation over them and they are creating circular dependency. 

137) Timeouts are applied wherever your system waits on external resources—HTTP calls, DB, threads, messaging—to prevent resource blocking and cascading failures.


138) -> Domain Decomposition (DDD) -> Single Responsibility per Service -> Loose Coupling & High Cohesion -> Database per Service -> Inter-Service Communication -> API Gateway -> Service Discovery -> Fault Tolerance & Resilience -> Observability (Monitoring, Logging, Tracing) -> CI/CD Pipeline -> Containerization & Orchestration -> Security

139) ec2 -> kafka,redis:: EKS -> pods ::  rds -> mySQL :: s3 -> reports attachments :: ElasticCatche :: redis :: Lamda :: background jobs,sceduled  , 

140) Feature → Dev → QA → UAT → Staging → Prod   {env}
         (auto)   (manual/trigger) (approval-based)

141) feature → develop → release → main    {branch} 
            		  ↓             ↓        ↓
           		 DEV          QA/UAT   STAGE/PROD

142) Helm is used to package and manage Kubernetes applications using charts.


143) // Service Test:
// - @ExtendWith(MockitoExtension.class)
// - Use @Mock and @InjectMocks
// - Use when().thenReturn()
// - Use assertEquals()

// Controller Test:
// - @WebMvcTest
// - Use MockMvc
// - Use @MockBean for service
// - Use mockMvc.perform().andExpect()


144) “A lambda expression provides the implementation of the abstract method of a functional interface. Unlike an anonymous class, it does not create a separate class or its own this context (it is implemented internally using invokedynamic, not a .class file). Because of this, this inside a lambda refers to the enclosing class instance.”

145)Shallow  -> same inner references
 Deep     -> completely independent objects
 Defensive-> copy to protect internal data

146) Write operations → @transaction is mandatory we can write on service or repo method

147) @Modifying is required only for custom update/delete queries in Spring Data JPA, not for standard repository methods like save or delete.

148) In production, we keep logs at INFO and ERROR levels. For debugging, we temporarily enable DEBUG for specific packages instead of globally, to avoid performance issues and excessive logging.

149) TRACE → DEBUG → INFO → WARN → ERROR

150) The actual resource is the underlying file (file descriptor). BufferedReader is just a wrapper over FileReader. Closing BufferedReader ensures the underlying file resource is also released.

151) Idempotency = deduplication + safe replay + atomic control

152) use csrf when -> Using session-based authentication -> Using cookies for login -> use csrf token
as we are not storing jwt in cookies as we are using stateless comm -> so 

153) A – Atomicity
C – Consistency
I – Isolation
D – Durability


154) transaction on method outside which is calling method inside.

155) strategy -> at runtime pick the correct class -> rule engine -> payment , authentication

156) factory -> Object creation is centralized

157) Behavioral Design Patterns -> Strategy, Observer
Creational Design Patterns -> Factory, Builder, Singleton, Prototype
Structural Design Patterns -> Adapter, Proxy



